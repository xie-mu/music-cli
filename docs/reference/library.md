# `nm library` commands

Index: [index.md](index.md)

| Command | Description |
|---|---|
| `nm library liked` | List liked songs for the current or specified user |

```bash
nm library liked
nm library liked --uid 123456 --output json
```

`nm library liked` requires login when `--uid` is omitted.
